# Titan OS Web V1 — Environment Variables
# ⚠️  NEVER commit .env.local or any file containing real secrets.
# Copy template values and fill with your own credentials.

# --- Supabase ---
NEXT_PUBLIC_SUPABASE_URL=https://dfvvzethuosbpqdslqtu.supabase.co
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=

# --- Database (Supabase PostgreSQL) ---
# DATABASE_URL = pooled/runtime connection (used by Prisma at runtime)
DATABASE_URL=
# DIRECT_URL = direct/session connection (used by Prisma for migrations/tooling)
DIRECT_URL=

# --- Next.js ---
NEXT_PUBLIC_APP_URL=http://localhost:3000
